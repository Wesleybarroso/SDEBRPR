import DashboardLayout from "@/components/DashboardLayout";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { Progress } from "@/components/ui/progress";
import { Activity, Award, CheckCircle2, MessageCircle, Sparkles, Target } from "lucide-react";
import { trpc } from "@/lib/trpc";

function Metric({ label, value, total, tone }: { label: string; value: number; total: number; tone: string }) {
  const percent = total ? Math.round((value / total) * 100) : 0;
  return <div className="space-y-2"><div className="flex items-center justify-between gap-3 text-sm"><span className="text-foreground/70">{label}</span><span className="font-semibold text-foreground">{value} <span className="text-xs font-normal text-muted-foreground">({percent}%)</span></span></div><Progress value={percent} className={`h-2 ${tone}`} /></div>;
}

export default function Qualidade() {
  const { data, isLoading, error } = trpc.dashboard.quality.useQuery();
  const quality = data ?? { total: 0, averageScore: 0, bands: { excellent: 0, good: 0, attention: 0, unscored: 0 }, qualification: { qualified: 0, discarded: 0, pending: 0 }, whatsapp: { valid: 0, invalid: 0, pending: 0 }, ready: 0 };
  return <DashboardLayout><div className="min-h-screen bg-background p-6"><div className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="text-[11px] font-semibold uppercase tracking-[0.2em] text-primary">Inteligência da operação</p><h1 className="mt-2 font-serif text-4xl text-foreground">Qualidade dos leads</h1><p className="mt-2 max-w-2xl text-sm text-muted-foreground">Entenda a força da sua base antes de investir tempo em validação e mensagens.</p></div><Badge className="w-fit border-0 bg-indigo-100 px-3 py-2 text-indigo-700"><Activity className="mr-2 h-4 w-4" />Dados em tempo real</Badge></div>{error && <div className="mt-6 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">Não foi possível carregar a qualidade dos leads.</div>}{isLoading ? <div className="mt-8 rounded-2xl bg-white p-8 text-sm text-muted-foreground shadow-sm">Calculando a qualidade da base...</div> : <><div className="mt-8 grid gap-4 md:grid-cols-2 xl:grid-cols-4"><Card className="border-0 bg-slate-950 text-white shadow-sm"><CardContent className="p-5"><Target className="h-6 w-6 text-indigo-200" /><p className="mt-6 text-sm text-slate-300">Score médio</p><p className="mt-1 font-serif text-4xl">{quality.averageScore}</p><p className="mt-1 text-xs text-slate-400">de 100 pontos</p></CardContent></Card><Card className="border-0 shadow-sm"><CardContent className="p-5"><Award className="h-6 w-6 text-amber-600" /><p className="mt-6 text-sm text-muted-foreground">Leads excelentes</p><p className="mt-1 font-serif text-4xl text-foreground">{quality.bands.excellent}</p><p className="mt-1 text-xs text-muted-foreground">score igual ou superior a 80</p></CardContent></Card><Card className="border-0 shadow-sm"><CardContent className="p-5"><MessageCircle className="h-6 w-6 text-emerald-600" /><p className="mt-6 text-sm text-muted-foreground">WhatsApp válido</p><p className="mt-1 font-serif text-4xl text-foreground">{quality.whatsapp.valid}</p><p className="mt-1 text-xs text-muted-foreground">de {quality.total} leads coletados</p></CardContent></Card><Card className="border-0 shadow-sm"><CardContent className="p-5"><CheckCircle2 className="h-6 w-6 text-violet-600" /><p className="mt-6 text-sm text-muted-foreground">Prontos para envio</p><p className="mt-1 font-serif text-4xl text-foreground">{quality.ready}</p><p className="mt-1 text-xs text-muted-foreground">qualificados e validados</p></CardContent></Card></div><div className="mt-6 grid gap-6 xl:grid-cols-2"><Card className="border-0 shadow-sm"><CardHeader><CardTitle className="flex items-center gap-2 font-serif text-2xl font-normal"><Sparkles className="h-5 w-5 text-amber-600" />Distribuição de score</CardTitle><p className="text-sm text-muted-foreground">A leitura mostra onde a base concentra maior potencial comercial.</p></CardHeader><CardContent className="space-y-5"><Metric label="Excelente · 80–100" value={quality.bands.excellent} total={quality.total} tone="[&>div]:bg-emerald-600" /><Metric label="Bom · 60–79" value={quality.bands.good} total={quality.total} tone="[&>div]:bg-amber-500" /><Metric label="Atenção · 1–59" value={quality.bands.attention} total={quality.total} tone="[&>div]:bg-orange-500" /><Metric label="Sem score" value={quality.bands.unscored} total={quality.total} tone="[&>div]:bg-slate-400" /></CardContent></Card><Card className="border-0 shadow-sm"><CardHeader><CardTitle className="font-serif text-2xl font-normal">Prontidão operacional</CardTitle><p className="text-sm text-muted-foreground">Acompanhe as etapas que antecedem o disparo via Evolution Go.</p></CardHeader><CardContent className="space-y-6"><div><p className="mb-3 text-xs font-semibold uppercase tracking-wider text-primary">Qualificação por IA</p><div className="grid gap-3 sm:grid-cols-3"><div className="rounded-xl bg-emerald-100 p-4"><p className="text-xs text-emerald-700">Qualificados</p><p className="mt-1 text-2xl font-semibold text-emerald-700">{quality.qualification.qualified}</p></div><div className="rounded-xl bg-amber-100 p-4"><p className="text-xs text-amber-700">Pendentes</p><p className="mt-1 text-2xl font-semibold text-amber-700">{quality.qualification.pending}</p></div><div className="rounded-xl bg-rose-100 p-4"><p className="text-xs text-rose-700">Descartados</p><p className="mt-1 text-2xl font-semibold text-rose-700">{quality.qualification.discarded}</p></div></div></div><div><p className="mb-3 text-xs font-semibold uppercase tracking-wider text-primary">Validação de WhatsApp</p><div className="grid gap-3 sm:grid-cols-3"><div><p className="text-sm text-foreground/70">Válidos</p><p className="text-xl font-semibold text-emerald-700">{quality.whatsapp.valid}</p></div><div><p className="text-sm text-foreground/70">Pendentes</p><p className="text-xl font-semibold text-amber-700">{quality.whatsapp.pending}</p></div><div><p className="text-sm text-foreground/70">Inválidos</p><p className="text-xl font-semibold text-rose-700">{quality.whatsapp.invalid}</p></div></div></div></CardContent></Card></div></>}</div></DashboardLayout>;
}
